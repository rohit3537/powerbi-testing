# Session Log — 2026-04-07

## What we worked on
- Client project (MESACO / Neetu): fixing PLACEHOLDER error on Power BI Service refresh
- Diagnosed 7 DAX measures with boolean expressions used as CALCULATE filter arguments
- Used TOM (Tabular Object Model) via pythonnet to programmatically fix all 7 measures in the live PBI Desktop model

## Decisions made
- Used TOM instead of TMSL because TMSL cannot target individual measures (only full table definitions)
- Found the TOM DLL at `C:\Program Files\On-premises data gateway\FabricIntegrationRuntime\5.0\Gateway\Microsoft.AnalysisServices.Tabular.dll`
- For the 6 Customer measures: split `&&` boolean in CALCULATE filter args into separate comma-delimited filter arguments
- For Production (count): replaced boolean measure reference `[Before Today (Date Filter)] > 0` with explicit `FILTER(ALL(...), condition)` table expression

## Current state
- All 7 measures fixed and verified in the live PBI Desktop model
- User needs to Ctrl+S in Power BI Desktop to persist changes to the .pbix file
- User needs to re-publish to Power BI Service and test refresh

## Measures fixed
1. `Customer > 10k (in 12m)` — table: PowerBI_Verkoopfactuurregels_Sales_Invoice_Lines
2. `Customer > 10k (12-24m)` — same table
3. `Customer 40k (in 12m)` — same table
4. `Customer 40k (12-24m)` — same table
5. `Customer 100k (in 12m)` — same table
6. `Customer 100k (12-24m)` — same table
7. `Production (count)` — table: PowerBI_Factuurtermijnen_Invoice Terms

## Next steps
- Confirm refresh works on Power BI Service after re-publish
- Consider running a full static review of remaining measures for other PBI Service incompatibilities

## Open questions
- Are there other workbooks from this client that might have the same pattern?
